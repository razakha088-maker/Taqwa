data class Quote(
    val bnTitle: String,
    val bnText: String,
    val enTitle: String,
    val enText: String
)

val quotes = listOf(

    Quote(
        "সহিহ বুখারি : ৬২৪৩",
        "রাসূলুল্লাহ ﷺ বলেছেন: 'দুই চোখের জিনা হলো (হারাম জিনিসের দিকে) তাকানো।'",
        "Sahih al-Bukhari 6243",
        "The adultery of the eyes is looking (at what is forbidden)."
    ),

    Quote(
        "সূরা নূর : ৩০",
        "মুমিন পুরুষদের বলুন, তারা যেন তাদের দৃষ্টিকে সংযত রাখে এবং তাদের লজ্জাস্থানের হেফাজত করে।",
        "Surah An-Nur : 30",
        "Tell the believing men to lower their gaze and guard their chastity."
    ),

    Quote(
        "সুনানে আবু দাউদ : ২১৪৯",
        "হে আলী! এক দৃষ্টির পর আরেক দৃষ্টি দিও না। প্রথমটি ক্ষমাযোগ্য, দ্বিতীয়টি নয়।",
        "Sunan Abu Dawud 2149",
        "Do not follow one glance with another."
    ),

    Quote(
        "সহিহ মুসলিম : ২১৫৯",
        "আমি আকস্মিক দৃষ্টি সম্পর্কে জিজ্ঞাসা করলে তিনি বললেন, 'তোমার দৃষ্টি ফিরিয়ে নাও।'",
        "Sahih Muslim 2159",
        "Turn your gaze away."
    ),

    Quote(
        "সূরা নূর : ৩১",
        "মুমিন নারীদেরকে বলুন, তারা যেন তাদের দৃষ্টিকে সংযত রাখে এবং তাদের লজ্জাস্থানের হেফাজত করে।",
        "Surah An-Nur : 31",
        "Tell the believing women to lower their gaze and guard their chastity."
    )

)
var currentQuote by remember { mutableIntStateOf(0) }
var isBangla by remember { mutableStateOf(true) }

LaunchedEffect(Unit) {
    while (true) {
        kotlinx.coroutines.delay(6000)
        currentQuote = (currentQuote + 1) % quotes.size
    }
}
Card(
    modifier = Modifier.fillMaxWidth(),
    shape = RoundedCornerShape(16.dp)
) {

    Column(
        modifier = Modifier.padding(20.dp)
    ) {

        Text(
            text = if (isBangla)
                quotes[currentQuote].bnTitle
            else
                quotes[currentQuote].enTitle,

            fontSize = 15.sp,
            color = Color(0xFF2E7D32)
        )

        Spacer(modifier = Modifier.height(8.dp))

        Text(
            text = if (isBangla)
                quotes[currentQuote].bnText
            else
                quotes[currentQuote].enText,

            fontSize = 18.sp
        )

    }
}

import androidx.compose.animation.AnimatedContent
import androidx.compose.animation.ExperimentalAnimationApi
import androidx.compose.animation.slideInHorizontally
import androidx.compose.animation.slideOutHorizontally
import androidx.compose.animation.togetherWith
import androidx.compose.material3.TextButton

@OptIn(ExperimentalAnimationApi::class)
@Composable
fun HomeScreen() {

AnimatedContent(
    targetState = currentQuote,
    transitionSpec = {
        slideInHorizontally { it } togetherWith
                slideOutHorizontally { -it }
    },
    label = "quote"
) { index ->

    Column {

        Text(
            text = if (isBangla)
                quotes[index].bnTitle
            else
                quotes[index].enTitle,
            color = Color(0xFF2E7D32),
            fontSize = 15.sp
        )

        Spacer(modifier = Modifier.height(8.dp))

        Text(
            text = if (isBangla)
                quotes[index].bnText
            else
                quotes[index].enText,
            fontSize = 18.sp,
            color = Color.White
        )
    }
}

Row(
    modifier = Modifier.fillMaxWidth(),
    horizontalArrangement = Arrangement.SpaceBetween,
    verticalAlignment = Alignment.CenterVertically
) {

    Text(
        text = "TAQWA",
        fontSize = 36.sp,
        color = Color.White
    )

    TextButton(
        onClick = {
            isBangla = !isBangla
        }
    ) {
        Text(
            if (isBangla) "English" else "বাংলা"
        )
    }

}

Spacer(modifier = Modifier.height(20.dp))

Card(
    modifier = Modifier.fillMaxWidth(),
    shape = RoundedCornerShape(20.dp),
    colors = CardDefaults.cardColors(
        containerColor = Color(0xFF111A13)
    )
) {

    Column(
        modifier = Modifier.padding(20.dp)
    ) {

        Text(
            text = "Protection Status",
            color = Color.Gray,
            fontSize = 16.sp
        )

        Spacer(modifier = Modifier.height(10.dp))

        Row(
            modifier = Modifier.fillMaxWidth(),
            horizontalArrangement = Arrangement.SpaceBetween,
            verticalAlignment = Alignment.CenterVertically
        ) {

            Column {

                Text(
                    text = if (enabled) "PROTECTED" else "NOT PROTECTED",
                    color = if (enabled) Color(0xFF4CAF50) else Color.Red,
                    fontSize = 24.sp
                )

                Spacer(modifier = Modifier.height(4.dp))

                Text(
                    text = if (enabled)
                        "Taqwa Guard is protecting your device"
                    else
                        "Protection is disabled",

                    color = Color.LightGray,
                    fontSize = 14.sp
                )

            }

            Switch(
                checked = enabled,
                onCheckedChange = {
                    enabled = it
                }
            )

        }

    }

}

Spacer(modifier = Modifier.height(20.dp))

Text(
    text = "Statistics",
    color = Color.White,
    fontSize = 22.sp
)

Spacer(modifier = Modifier.height(12.dp))

Row(
    modifier = Modifier.fillMaxWidth(),
    horizontalArrangement = Arrangement.spacedBy(12.dp)
) {

    StatCard(
        title = "Images",
        value = "0"
    )

    StatCard(
        title = "Videos",
        value = "0"
    )

}

Spacer(modifier = Modifier.height(12.dp))

Row(
    modifier = Modifier.fillMaxWidth(),
    horizontalArrangement = Arrangement.spacedBy(12.dp)
) {

    StatCard(
        title = "Detections",
        value = "0"
    )

    StatCard(
        title = "Today",
        value = "0m"
    )

}


@Composable
fun StatCard(
    title: String,
    value: String
) {

    Card(
        modifier = Modifier.weight(1f),
        colors = CardDefaults.cardColors(
            containerColor = Color(0xFF111A13)
        ),
        shape = RoundedCornerShape(18.dp)
    ) {

        Column(
            modifier = Modifier.padding(18.dp),
            horizontalAlignment = Alignment.CenterHorizontally
        ) {

            Text(
                text = value,
                color = Color(0xFF4CAF50),
                fontSize = 26.sp
            )

            Spacer(modifier = Modifier.height(6.dp))

            Text(
                text = title,
                color = Color.LightGray,
                fontSize = 14.sp
            )

        }

    }

}


Spacer(modifier = Modifier.height(20.dp))

Text(
    text = "Detection Mode",
    color = Color.White,
    fontSize = 22.sp
)

Spacer(modifier = Modifier.height(12.dp))

var selectedMode by remember {
    mutableStateOf("Explicit")
}

Row(
    modifier = Modifier.fillMaxWidth(),
    horizontalArrangement = Arrangement.spacedBy(12.dp)
) {

    ModeCard(
        title = "Explicit",
        description = "Explicit Content Only",
        selected = selectedMode == "Explicit"
    ) {
        selectedMode = "Explicit"
    }

    ModeCard(
        title = "Women",
        description = "Women Detection",
        selected = selectedMode == "Women"
    ) {
        selectedMode = "Women"
    }

    ModeCard(
        title = "Both",
        description = "Both Modes",
        selected = selectedMode == "Both"
    ) {
        selectedMode = "Both"
    }

}



@Composable
fun ModeCard(
    title: String,
    description: String,
    selected: Boolean,
    onClick: () -> Unit
) {

    Card(
        modifier = Modifier
            .width(140.dp)
            .height(180.dp)
            .clickable { onClick() },
        shape = RoundedCornerShape(18.dp),
        colors = CardDefaults.cardColors(
            containerColor =
            if (selected)
                Color(0xFF1B2A1B)
            else
                Color(0xFF111A13)
        )
    ) {

        Column(
            modifier = Modifier
                .fillMaxSize()
                .padding(16.dp),
            horizontalAlignment = Alignment.CenterHorizontally,
            verticalArrangement = Arrangement.Center
        ) {

            Text(
                text = title,
                color = Color(0xFF67E06F),
                fontSize = 20.sp
            )

            Spacer(modifier = Modifier.height(12.dp))

            Text(
                text = description,
                color = Color.White,
                fontSize = 14.sp
            )

        }

    }

}


Spacer(modifier = Modifier.height(20.dp))

Text(
    text = "Quick Actions",
    color = Color.White,
    fontSize = 22.sp
)

Spacer(modifier = Modifier.height(12.dp))

Row(
    modifier = Modifier.fillMaxWidth(),
    horizontalArrangement = Arrangement.spacedBy(12.dp)
) {

    ActionCard("Quick Toggle")
    ActionCard("Sensitivity")

}

Spacer(modifier = Modifier.height(12.dp))

Row(
    modifier = Modifier.fillMaxWidth(),
    horizontalArrangement = Arrangement.spacedBy(12.dp)
) {

    ActionCard("Allowed Apps")
    ActionCard("Statistics")

}

@Composable
fun ActionCard(
    title: String
) {

    Card(
        modifier = Modifier
            .weight(1f)
            .height(110.dp),
        shape = RoundedCornerShape(18.dp),
        colors = CardDefaults.cardColors(
            containerColor = Color(0xFF111A13)
        )
    ) {

        Box(
            modifier = Modifier.fillMaxSize(),
            contentAlignment = Alignment.Center
        ) {

            Text(
                text = title,
                color = Color.White,
                fontSize = 16.sp
            )

        }

    }

}


Spacer(modifier = Modifier.height(24.dp))

NavigationBar(
    containerColor = Color(0xFF111A13)
) {

    NavigationBarItem(
        selected = true,
        onClick = { },
        icon = {
            Text("🏠")
        },
        label = {
            Text("Home")
        }
    )

    NavigationBarItem(
        selected = false,
        onClick = { },
        icon = {
            Text("🛡️")
        },
        label = {
            Text("Protection")
        }
    )

    NavigationBarItem(
        selected = false,
        onClick = { },
        icon = {
            Text("📊")
        },
        label = {
            Text("Insights")
        }
    )

    NavigationBarItem(
        selected = false,
        onClick = { },
        icon = {
            Text("⚙️")
        },
        label = {
            Text("Settings")
        }
    )
}
